# Basic but nice OpenTimelineIO converter

This is a first step to try to write a "nice" (user freindly) OTIO command line converter, that guides user as most as possible.

## Usage:

```
otio_converter.py --help
```
